#1.16.5버전용(적용이 않될수 있음 나중에 곧치겠습니다!)
@SubscribeEvent
public void onEntityDamage(LivingHurtEvent event) {
    // 플레이어인지 확인
    if (event.getEntity() instanceof Player player) {
        // 랜덤한 이동 속도 설정
        double speed = 0.1 + Math.random() * 0.5; // 0.1 ~ 0.6 사이 랜덤값
        player.getAttribute(Attributes.MOVEMENT_SPEED).setBaseValue(speed);

        // 플레이어에게 효과 알림 (선택 사항)
        player.sendMessage(new StringTextComponent("이동 속도가 변경되었습니다!"), player.getUUID());
    }
}
